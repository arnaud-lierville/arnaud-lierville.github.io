TODO :

python -m SimpleHTTPServer

/*
larg et long du plan de travail
Options et paramètres dans les fonctions
config (stairs, carpet)
Commandes : affichage des shortcuts
log
refactor

Mise en ligne
*/

Number.prototype.countDecimals = function () {
    if(Math.floor(this.valueOf()) === this.valueOf()) return 0;
    return this.toString().split(".")[1].length || 0; 
};
